@model GestaodeFrota.Models.Posto
@{
    ViewData["Title"] = "Posto";
}

<h2>@ViewData["Title"]</h2>

<h3>Tem certeza que vôce quer Deletar?</h3>

<div>
    <h4>Posto</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.NomePosto)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.NomePosto)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.EnderecoPosto)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.EnderecoPosto)
        </dd>
    </dl>

    <form asp-action="DeletePosto">
        <input typy="hidden" asp-for="Id" />
        <input type="submit" value="Deletar" class="btn btn-danger" />
        <a asp-action="Index">Cancelar</a>
    </form>
</div>